<% include header %>

    <div class="login-card">
      <h1><%= title %></h1>
      <% if (locals.flash) { %>
        <p><%= flash %></p>
      <% } %>

      <!-- TODO 
        include qr_exchange 
      -->

      <!-- TODO 
        this is a placeholder until proper did-auth exchange is implemented
      -->
      <form autocomplete="off" action="/interaction/<%= uid %>/login" method="post">
        <input required type="text" name="accountId" placeholder="Enter your DID" <% if (!params.login_hint) { %>autofocus="on"<% } else { %> value="<%= params.login_hint %>" <% } %>>
        <button type="submit" class="login login-submit">Submit</button>
      </form>
    </div>
 
<% include footer %>